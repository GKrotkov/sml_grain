---
title: "Model Training"
output: pdf_document
---

# Model Training
```{r}
# Using LASSO since it yielded a smaller MSE
model <- glmnet(x_train, y_train, alpha = 1, lambda = lasso_model$lambda.min)
```

